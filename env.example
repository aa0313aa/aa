# Development/example environment variables
DATABASE_URL="file:./dev.db"
SITE_URL="http://localhost:3000"

# Mail (Ethereal in dev, replace in prod)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=

# Auth
JWT_SECRET=replace-me-with-a-secure-random-string

# Optional
NEXT_PUBLIC_BASE_URL=${SITE_URL}
