# Production environment variables - copy values into Vercel/Settings or GitHub Secrets
# DO NOT commit real secrets to the repository. Use this file only as a template.

# Database (Postgres / PlanetScale / Neon / Supabase etc.)
DATABASE_URL="postgresql://DB_USER:DB_PASS@db-host.example.com:5432/aa_prod?schema=public"

# JWT secret (generate a secure random 32+ byte hex string)
JWT_SECRET=replace-with-strong-random-value

# Email / SMTP (for verification and password reset)
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASS=replace-with-smtp-password-or-api-key
EMAIL_FROM="no-reply@yourdomain.com"

# Public site URL
NEXT_PUBLIC_BASE_URL="https://your-deployment-url.vercel.app"

# Optional: tweak Node environment
NODE_ENV=production
